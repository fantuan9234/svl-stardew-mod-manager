# Changelog

## [1.0.6] - 2026-05-05

### Features

- **MOD 列表分组显示**：大型 MOD（如 SVE）包含多个子包时，在列表中聚合为单个主条目，避免列表膨胀
- **子包详情 Modal**：点击分组主条目后弹出半屏面板，以表格形式列出所有子包（名称、版本、作者、依赖数量、启用状态）
- **分组 MOD 交互**：分组条目支持启用/禁用切换、卸载、冲突提示，与普通 MOD 保持一致的操作体验

### Bug Fixes

- **修复 SVE 被拆分为多个独立条目的问题**：当顶层文件夹无 `manifest.json` 但包含多个子 MOD 时，自动创建分组条目，而非平铺展示
- **单嵌套 MOD 保持原样**：当顶层文件夹只包含一个子 MOD 时，仍作为普通 MOD 展示，不创建分组

## [1.0.5] - 2026-05-05

### Features

- **ContentPacks 依赖解析支持**：新增 `ContentPacks` 字段解析，递归收集所有子包的 `Dependencies` 并与顶层依赖合并去重
- **依赖统计日志增强**：解析完成后打印收集到的依赖总数，方便验证 SVE 等大型 MOD 的依赖解析结果

### Bug Fixes

- **修复 SVE 等大型 MOD 依赖显示为"未声明"的问题**：
  - 为 `ContentPacks` 字段添加 `#[serde(default)]`，缺失时默认为空数组
  - 遍历 `ContentPacks` 数组，收集每个子包的 `Dependencies`，与顶层依赖合并去重
  - 解析完成后打印收集到的依赖总数，方便验证
- **修复前端依赖展示文案**：
  - 依赖解析成功但为空时显示"依赖关系：无依赖"，不再显示"未声明"
  - "未声明"提示仅在 manifest.json 完全不存在或完全无法解析时才出现
  - 依赖解析成功且有内容时显示"依赖关系：共 X 个（必选 Y 个，缺失 Z 个）"

## [1.0.4] - 2026-05-05

### Features

- **冲突检测与前置依赖检查功能分离**：两个卡片现在展示不同范围的结果，不再完全重复
- **冲突检测**：展示所有类型问题（必选依赖缺失、可选依赖缺失、ContentPack 冲突、已知不兼容），每项附带解决方案按钮
- **前置依赖检查**：仅展示必选依赖（`IsRequired: true`）的缺失项，不展示可选依赖和 ContentPack 冲突
- **前置依赖检查卡片增强**：新增"查看解决方案"按钮，与冲突检测卡片保持一致的交互体验

### Bug Fixes

- **修复前置依赖检查字符串解析问题**：移除 `handleDepScan` 中正则表达式解析 `description` 的脆弱逻辑，直接使用后端返回的 `ConflictReport` 数据
- **修复 MOD 依赖解析引擎**：为 `Dependencies` 字段添加 `#[serde(default)]`，确保 JSON 中该字段为 null、[] 或缺失时都能正确解析
- **修复 IsRequired 默认值问题**：缺失时自动默认为 `true`（必选），符合 SMAPI 规范
- **修复 Content Patcher 内容包隐式依赖**：自动为 `ContentPackFor` 指向 Content Patcher 的 MOD 追加隐式依赖
- **修复 MOD 详情面板依赖展示**：依赖列表为空时显示"未声明（可能为内容包）"，不再显示容易引起误解的"依赖关系（0）"

## [1.0.3] - 2026-05-04

### Features

- **MOD 详情依赖面板优化**：动态统计缺失必选前置数量，显示"缺失 N 个必选前置 (总共 Y 个依赖)"
- **依赖安装状态徽章**：每个依赖项右侧显示"已安装"（绿色）或"缺失"（红色）状态
- **依赖筛选功能**：新增全部/必选/可选/缺失筛选按钮组
- **统一 URL 打开封装**：新建 `openUrl.ts` 工具函数，统一处理 URL 标准化、编码和错误提示
- **UpdateKeys 解析优化**：后端只提取纯数字 MOD ID，自动丢弃 `@IF2R` 等非法字符
- **Nexus 搜索链接降级**：无法提取有效 MOD ID 时，自动使用 MOD 名称构建搜索链接

### Bug Fixes

- **修复 MOD 链接 404 问题**：`Nexus:3753@IF2R` 等格式现在正确解析为 `mods/3753`
- **修复 shell.open 无响应问题**：统一使用 `@tauri-apps/plugin-opener` 的 `openUrl` 方法
- **修复前端链接打开失败**：所有链接按钮统一使用封装的 `openUrl` 函数，失败时弹出明确提示
- **修复搜索链接格式**：统一使用 `/mods/search?search=` 格式，替代旧的 `/?gsearch=` 格式

## [1.0.2] - 2026-05-04

### Bug Fixes

- **修复 Rust 后端潜在 panic 风险**：替换 `log_parser.rs` 中 `Regex::new().unwrap()` 为 `LazyLock` 预编译正则表达式，避免运行时 panic
- **修复临时文件清理失败问题**：`mod_installer.rs` 中新增重试机制（最多 3 次），确保临时目录清理成功
- **修复配置文件加载异常**：`profiles.rs` 中替换 `unwrap_or_default()` 为完整错误处理，避免配置损坏时崩溃
- **修复 SMAPI 启动后临时文件残留**：`smapi_launcher.rs` 中增加 `cleanup_temp_mods()` 调用
- **修复前端国际化缺失**：替换 `ModManager.tsx` 中硬编码中文文本为 `t()` 翻译键
- **修复组件内存泄漏风险**：`DropZone.tsx` 中增加事件监听器清理安全检查，防止重复 unlisten
- **修复窗口事件监听未捕获异常**：`AppLayout.tsx` 中为窗口事件增加 `.catch()` 处理
- **修复健康检查页面空 useEffect**：移除 `HealthCheck.tsx` 中无用的空 useEffect
- **修复未使用字段编译警告**：为未使用的结构体字段添加 `#[allow(dead_code)]` 注解
- **优化打包体积**：`tauri.conf.json` 中排除开发文件，减少安装包大小

### Features

- **SMAPI 检测引导增强**：增加游戏根目录大白话解释，提供"帮我找到游戏目录"按钮
- **日志解读器增强**：新增"套娃错误"和"缺少 manifest.json"错误识别及白话解决方案
- **冲突检测增加 N 网搜索**：缺失依赖时提供"在 Nexus 搜索"按钮
- **新增常见问题速查**：MOD 健康页面增加 Collapse 面板展示常见术语白话解释
